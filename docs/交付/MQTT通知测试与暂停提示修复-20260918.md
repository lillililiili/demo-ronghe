# MQTT 通知测试与暂停提示修复（2026-09-18）

## 页面操作

告警事件选择 **告警-0918-003**，区域为 `MQTT隔离模拟区域 notice-0918-f`，目标为 `目标-20260918-004`。本次已通过页面提交明确标记为模拟验收的核实记录；短信显示“已送达 / 模拟”，人工补充现场情况入口可用。现场补充弹窗已打开核对并取消，没有补造飞离、危险等级、反制执行或处罚结果。

旧告警不再因为短信资格受阻而整行显示“自动通知已暂停”。详情改为“暂不满足发送条件”，展开实际原因；核实不会更新原告警或观测时间。已有现场记录、联系及通知事实继续按原进度显示。

## 本次修复

- 告警核实状态与通知资格分开展示；过期原因说明如何用新的模拟场景测试。
- 后端 MERGED/DOWNGRADED 新研判关联归并当时已有的告警，解决最新违规依据无法通过通知事件关联校验的问题。不增加重复告警，不修改旧告警时间；合并成员的新建语义保持原样。
- 本地调度使用 4 线程、每批最多 5 帧，并优先领取仍在上报的来源。同一来源保留先后顺序，历史积压不删除；生产默认不启用来源优先。
- 新增 `../houtaiguanli/server/scripts/mqtt_notification_scenario.py`，通过现有 API 和受控前置数据准备独立模拟区域、计划、临时禁飞区、飞手及本机 replay 连接。雷达/TDOA MQTT 报文驱动后端产生观测、研判、告警和模拟短信回执。

## 已验证业务事实

| 项目 | 标识 / 结果 |
|---|---|
| 批次 | notice-0918-f |
| 目标 | dca9b776-d846-4008-81fb-646335649cf4 |
| 计划 | e9048373-4759-5a9d-83ad-23c9c6f84d26 |
| 告警 | ad580bde-cf2c-4656-a70a-2e19c66da893 / 告警-0918-003 |
| 事件 | 5d3d62ac-2cbc-4e4f-84f5-582529993a2c / CONFIRMED |
| 触发通知研判 | 8da0bb19-a4c0-4e44-88b4-ee8e8d0bff6e |
| 短信回执 | a12c6fd9-3dfe-43d5-91a2-3fb90761b51e / SIMULATED_DELIVERED |
| 发送次数 | 1；页面刷新、人工核实后仍为 1 |

批次于 2026-09-18 07:23:44 UTC 启动，有限运行 30 分钟。中途为本机负载将发送间隔调整为 2 秒，并在原有效期内恢复上报；未修改旧时间，停发或重启间隙不作为飞离证据。正常结束约为 07:53:42 UTC（北京时间 15:53:42）。过期后需要新建批次，不能通过核实旧告警重置通知窗口。

过程备份、原通知配置、前置资料、发布记录及验收结果位于 Windows 临时目录 `uav-notification-scenarios/notice-0918-f/`。诊断批次 a–e 的失败/不充分事实保留，其中 d/e 独立 MQTT 连接已停用；没有将它们的通知改成成功。

## 验证与边界

- 前台构建、源码扫描及旧地址迁移测试通过；浏览器已核对告警详情、模拟送达、核实及现场补充入口。
- 后端原短信策略、调度、PostgreSQL 接口/迁移和融合领取测试共 33 项通过；新增归并关联 H2/PostgreSQL 测试 2 项、规则回归 3 项通过。
- 后端构建通过；没有新增迁移或修改已应用迁移。管理端源码未发现本次研判告警字段的消费者，没有修改管理端页面。
- 两个仓库 `git diff --check` 通过。用户同期的反制办理页面、AGENTS 和相关文档修改保留，本轮未提交代码。
- 队列优先查询改用来源元组集合匹配，H2 与 PostgreSQL 专项测试重新通过，避免关联子查询触发昂贵 JIT 计划。服务重启后核实结果、模拟短信回执及发送次数 1 均保留。
- 强制重启期间一条重复观测命中既有唯一约束，保留为 FAILED；后续报文继续消费。未删除失败帧或将其改成成功。本次不将此视为所有重启/重复消息场景已全面通过。

验收范围为 **MQTT → 后端研判与告警 → 模拟短信 → 人工核实 → 现场补充入口可用**。真实短信、电话录音、真实设备反制、自动飞离解除、自动移送和处罚办结没有在本次接通或验收。当前引擎单源置信度及位置轨迹质量仍可能使后续研判显示不可判定；它不撤销已发生的模拟送达，也不等于风险解除。

再次生成新场景：在 `E:\沉积岩\houtaiguanli\server` 运行 `python scripts/mqtt_notification_scenario.py --seconds 1800`。已有其他新鲜告警时，工具会拒绝改动共享短信配置；请保留现场并确认测试范围，不放宽时效或强行修改历史记录。
