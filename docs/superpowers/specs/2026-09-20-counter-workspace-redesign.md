# 反制办理三栏改版

2026-09-20：用户要求重新设计反制办理，明确风格与其他页面一致。采用前一轮讨论的队列、目标观察、办理操作三栏，复用现有主题 token、字体、按钮、地图与急停组件。

## 展示与数据

- 左侧保留待办/全部筛选及同源分页；记录展示目标、动作、来源、当前进展、申请时间与下一步，点击只读详情，列表保持位置。
- 中间默认展开共享视频状态，无任务时紧凑提示；下方读取相同 target_id 的末次可信位置和最新轨迹完整分页，保留轨迹断点、后端航线关系、模拟/回放标识和观测时间。没有新接入视频流，不使用静态或模拟图冒充直播。
- 右侧集中当前进展、结果、阻断原因和已有审批/执行动作，急停/撤销使用原组件；授权资料与原始结果按需展开。旧人工执行通道保持只读，未知、失败、无权限不按成功处理。
- 只有显式点击业务动作才进入原办理流程。加载、切换和刷新不申请、不审批、不执行、不急停。没有更改后台资格、数据范围和状态机。
- 记录切换避免重复加载列表。地图随目标切换销毁，过期异步响应不更新新目标；告警页签地图在离开页签时释放，返回再创建。位置由用户刷新后重新读取，显示末次观测时间，不伪称持续实时更新。
- 顶部统计口径不变，仅在反制工作区压缩卡片高度；取消重复的大标题。长文字换行，列表、目标观察与资料分别局部滚动。

## 代码清单

| 文件 | 方法/功能 |
|---|---|
| src/pages/alarms/AuthorizationQueue.vue | 三栏模板与局部样式；refresh/show 区分只读切换与列表重载；selectedSubject 绑定当前关联目标 |
| src/pages/alarms/AuthorizationTargetMap.vue | load/locate/destroyMap：读取目标和最新完整轨迹、定位与销毁地图 |
| src/components/video/TargetLiveVideo.vue | defaultExpanded/compact 可选展示参数，展开监听首次读取；旧页面默认行为不变 |
| src/pages/AlarmsPage.vue | syncAlarmMap 与页签 watcher 管理地图生命周期；反制工作区专用紧凑统计样式 |

## 本次验证

- 生产构建通过，保留项目既有大分包提示；源码扫描 163 文件通过；旧管理地址迁移回归 1 项通过，调用已安装 Playwright 的 Node CLI；git diff --check 通过。
- 浏览器验证待我处理空态、全部记录、当前待审批申请、只读历史记录、记录切换、授权资料展开和关联告警返回。未点击审批、执行、撤销、急停等业务写操作。
- 1280×720、1366×768、1440×900 检查新增三栏；document 宽度均未溢出。1280 高度有限时中间观测信息通过区域内滚动完整查看，保留按钮与状态文字。关联告警导航另在 1440×1024 验证。
- 选中记录与页签切换后只存在一幅 MapLibre 地图；控制台 error 记录为空。真实视频源和设备执行不属于本轮验收。
- 效果图：/Users/frank/.codex/visualizations/2026/09/20/01a0bcae-5fd5-7fc0-8be5-ad8dc9277e58/counter-workspace-redesign.png（1440×900，当前本地回放数据）。

## 后续确认

2026-09-20 用户后续要求已替代本文初版的紧凑统计方案：顶部统计卡在两个页签均保持完整尺寸，不压缩为单行窄条。当前实现与两级 AGENTS.md、README 一致；上文紧凑统计描述仅记录初版过程。
