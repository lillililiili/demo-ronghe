# 设备信息 MQTT 模拟器

根据地图页面设计实现的独立本机工具。通过现有后端注册模拟设备，用 MQTT 向现有接入、融合与规则链路发送报文；不新建平台后端、不改业务前台路由、不直接填造告警。

## 启动

```sh
python3 tools/device-simulator/server.py --port 8766 --database houtaiguanli
```

打开 http://127.0.0.1:8766/ 。需 Python 3.9+、Node.js 及业务前台已有依赖；MQTT 使用现有 `paho-mqtt` 2.x，未新增项目生产依赖。启动时自动构建共享地图入口。

地图读取当前业务前台的同一配置、PMTiles 数据包、影像与配色。默认地图服务为 `http://localhost:5173`，需保持业务前台地图服务运行；端口变化时使用 `--map-origin http://localhost:实际端口`。地图资源仅经本机同源代理读取，保留 HTTP Range 206，不复制整份地图包；读取失败显示错误和重试入口。

启动终端需保持运行。若页面提示“模拟器服务未连接”，重新执行上述启动命令；页面会自动恢复状态读取。重启后在“登录系统”重新登录一次，历史批次保留且不会自动续发。提交操作时断连表示结果未知，先恢复连接并核对运行记录，再决定是否重新操作。

`--database` 显式启用本机测试库的计划、区域配套，默认容器 `deploy-db-1`（可用 `--container` 指定）。未传该参数仍可运行无需计划/区域的设备与目标场景。只接受回环 API 与已启用、source_mode=replay 的本机 MQTT 连接；不能用于现场/生产环境。

## 怎么操作

1. **连接设置**：填现有系统 API（本机为 `http://127.0.0.1:8081/api/v1`）、账号和密码。读取后台 `local-lingyun-replay`；如 broker 需要认证，另填 MQTT 用户名密码。凭据仅保存在进程内存，不写场景、日志或浏览器存储。计划/区域受控 seed 只允许测试系统管理员。
2. **地图编排**：地图上方分为“设备与目标”“航线与区域”“计划与气象输入”；左侧按任务显示对象，右侧编辑当前对象。风险列表默认收起，可通过“展开列表”配置。在系统真实地图上拖动、缩放，放置设备组（气象、5G-A、光电、TDOA、雷达），修改数量；绘制目标轨迹、计划航线与限飞区域。点击左侧对象定位并在右侧应用设置；“全览”回到当前场景范围。设备经纬度使用 WGS84，单台、追加、整组移动均保留。
3. **风险场景**：勾选本次实际发送的场景，指定目标、上报设备以及必要的计划/区域。取消勾选仅影响本次目标/异常发送，组内支持协议的设备仍上报正常心跳。
4. **启动 MQTT 模拟**：冻结场景快照，为新批次配套 replay 设备与资料，然后定速沿轨迹发送。运行 30 秒至 20 分钟后结束；暂停停止全部上报、继续沿原模拟进度运行；停止等待在途发布退出。关闭网页不停止 Python 进程内的运行任务。
5. **运行记录 → 回读系统结果**：分别查看 Broker PUBACK、后台设备事实、本批次目标、研判与告警。空结果不补成功，读取失败保留错误。源设备通过 `fusion_device_id` 对应目标 `source_links.device_id`。
6. **导出场景 / 导入场景 / 导出本批次**：场景可复用；批次导出包含设备/计划/区域映射、最近发送记录和系统回读。完整报文保存在本机 `.data/<batch>/events.ndjson`。

运行时锁定编辑器；停止后可以修改。保存场景仅保存编辑草稿，不会启动发送。未完成地图绘制时不能启动。

## 数据流与保护

- 设备注册：现有 `POST /api/v1/devices/onboard`，每次运行独立设备编号及幂等键，source_mode=replay。
- 雷达、5G-A、TDOA：复用 `LINGYUN_MQTT_V8_6` 工参和 SenseData 契约。QoS 1、retain=false；目标与批次身份固定，观测时间随实际发送更新，按速度和折线距离推进，终点停留，不循环瞬移。鸟群按数量生成独立观测编号、固定小范围编队偏移。
- 光电：`EO_EDGE_MQTT_20250826` HeartBeat。不会虚构跟踪任务、实时视频或控制执行回执。
- 离线：时间窗内停止该设备所有上报，恢复后正常上报；系统按自身超时阈值判离线。故障：协议 A workState=2，窗口结束后恢复原配置。
- 计划/区域：现有计划接口没有通用写入口，空域创建接口固定为 live。因此复用项目本机模拟脚本的受控 seed 做法，通过 PostgreSQL 事务仅新增 replay 航线、版本、计划及限飞区域。执行前以 broker UUID 核对 API 与数据库一致。完整场景快照在任何写入前落盘，不覆盖或删除旧记录。
- 计划、区域时段使用**运行当天 Asia/Shanghai**。普通场景遵守页面时间；“超出计划时段”按选择的提前/延后及分钟数，将该目标配套计划定位到本批次开始前后，实际生效时刻可从系统计划回读。
- 一条编辑航线可为不同无人机生成各自绑定序列号的计划；鸟群关联的计划用于航线风险，不伪造鸟群飞行计划匹配。
- 暂停可能使系统判设备离线，停止不代表风险解除。发送失败或 PUBACK 超时会停止任务，结果未知不自动重发。服务重启恢复上次批次记录，**不自动续发**。新运行使用新批次。
- 这不是平台规则引擎；不通过前端定时器生成系统告警、通知成功或反制成功。实际状态、合法性判定和告警均以系统回读为准。
- 2026-09-23：可视地图已替换为系统共用的真实底图，复用 `offlineMap.js`、`mapTheme.js` 与地图 tokens。旧 version=1 场景的内部 x/y 保留原 WGS84 换算，渲染时使用地图投影，因此原已发送位置不漂移；新选点不再受旧示意画布范围限制。重载已保存的草稿时，已完成批次不会覆盖草稿；运行中的批次仍显示其冻结快照。

## 当前协议边界

| 类型 / 场景 | 当前能力 |
| --- | --- |
| 雷达、5G-A、TDOA | 注册、心跳、工参、无人机/鸟类轨迹、离线与故障调度 |
| 光电 | 注册、心跳与离线；具体故障码与跟踪任务回执未接入，阻止伪造 |
| 气象设备 | 保留地图布设；现有系统没有相应设备 MQTT 契约，明确跳过发送；不伪造温度、风速字段 |
| 无计划、偏离、限飞区、时间风险 | 发送实际观测及配套资料，结果由现有引擎判断；选中场景不保证产生某个结论 |
| 超高 | 上报协议原始高度；当前后端将其基准视为未知，不能保证触发超高告警 |
| 气球 | 当前协议码表无气球分类；选中时阻止启动，不冒充鸟类或其他类别 |

## 文件与验证

- `engine.py`：冻结与校验场景、几何距离、运动插值、异常时间窗和 A/C 报文。
- `platform_client.py`：系统登录/回读、设备注册、本机配套事务。
- `server.py`：同源本地服务、任务线程、生命周期、日志、结果回读；仅绑定 127.0.0.1。
- `web/`：沿用确认的页面；`runtime.js` 接入实际运行、配置与导入导出。
- `map-entry.js` / `build-map.mjs`：共用系统地图加载器和主题，真实坐标投影、平移缩放及构建；`web/map-runtime/` 为自动生成资源，不提交。
- `tests/test_engine.py`：定速转弯、离线停报及恢复、故障恢复、数据校验、输入快照与不支持的协议阻断。

```sh
PYTHONPYCACHEPREFIX=/tmp/simulator-pycache python3 -m unittest discover -s tools/device-simulator/tests -v
node --check tools/device-simulator/web/app.js
node --check tools/device-simulator/web/runtime.js
```

最初 MQTT 接入轮次未运行业务前台 build/scan/admin-migration；2026-09-23 地图替换轮次单独执行这些检查。真实现场设备、短信和电话通道未验收。

## 本机链路验证（2026-09-22）

- `sim-0922052908-7c71`：30 秒，103 个 Broker PUBACK；后台目标报文 29 条 DONE。
- `sim-0922053543-e040`：30 秒，4 台设备、1 条计划、1 个限飞区域；103 个 Broker PUBACK，后台目标报文 28 条 DONE。回读得到 1 个融合目标、6 条研判、0 条告警，系统结论为 UNDETERMINED（不可判定）。未将这种结果改写为“告警成功”。
- 两批次均已停止，模拟历史保留。停止模拟后设备可按系统时效转离线；这不是发送任务仍在运行。

## 五组演示样本（2026-09-23）

页面底部“载入演示样本”会配置五组勾选场景：沿计划飞行后偏航、禁飞区闯入、无匹配计划、鸟群进入航线、雷达故障后恢复。运行时长 15 分钟；现有草稿保留在“恢复载入前草稿”。载入只编辑草稿，点击“启动 MQTT 模拟”才新增批次并发送。场景文件为 `web/demo-samples.json`，可以导出再导入。

- 4 台设备：无人机雷达、身份 TDOA、鸟群雷达、独立故障雷达；3 条计划、1 个禁飞区、3 架无人机和 3 只鸟的观测。融合后的目标数量由后端决定，不能按观测数强配。
- 无人机可选择一个辅助上报设备，两个来源共用目标序列号和轨迹；旧场景不配置时保持单设备。模拟离地高度 `heightAgl` 映射协议 `height`，模拟识别概率 `probability` 映射对应扩展字段；留空时不补值。识别概率不代表合法性可靠率。
- 样本计划与区域显式使用 AGL，目标离地高度 60 米。原始 `altitude` 仍按未知基准保留。旧场景未声明 `altitudeDatum` 时继续使用原 AMSL 配套，不能自动互相转换。
- 设备故障在第 60 秒开始，120 秒后恢复工参。心跳恢复不直接证明运维事件办结。
- 当天计划/区域有效时间为北京时间 00:00—23:59；停止发送不撤销资料或删除历史。每日新运行创建新批次，跨天需重新运行。
- 首次样本验收只验证发送与回读，未启用通知或反制；当时的融合与计划匹配阻塞记录保留在旧报告。后续“整个流程跑通”验收已修复异步来源组合、无关帧误推进缺测及融合身份未供计划匹配的问题，详见下方续验。
- 鸟群风险需要后台 C04 评估。首次验收使用受控接口手动评估；后续续验启用本机 C04 定时评估，二者分别留证。

### 全流程续验（2026-09-23，停止后回读于 2026-09-24 UTC）

- 最终批次 `sim-0923035711-e377` 已运行满 900 秒，4310 次发布确认，状态 COMPLETED；服务重启后只恢复记录，不续发。运维完整分支使用前一诊断批次 `sim-0923034655-1a7c`，两批历史均保留。
- 禁飞告警 `告警-0923-011`：核实 → 模拟短信 → 10 秒观察 → 模拟电话录音 → 10 秒观察 → 人工申请、另一账号审批 → 本机 CM4 协议模拟回执 → 关联干扰 → 自动处罚交接，送达、签收均为 MOCK。处罚尚未办结。
- 偏航先 LEGAL/FULL/SUFFICIENT，随后 ABNORMAL，再 ILLEGAL；证据不足状态保持复核。无计划未核实、未发送通知、不可反制。鸟群进入独立风险流程，通知已模拟送达，签收仍 PENDING；不进入无人机合法性页。雷达 workState 1→2→1 后完成运维通知与处理。
- 样本可显式填写 `pilotPoint`，仅将提供的模拟位置发送为 `pilotLon/pilotLat`，不补造缺失飞手位置。预置几何缩至原范围的 20%，速度 4m/s，避免额外触发本次五组之外、尚缺严重度配置的 BVLOS 规则；旧批次快照不变。
- 完整通知链需要有效的计划执行飞手、本机 MOCK 设置、录音配置与后台任务。点击载入样本只装载观测场景，不会自动配置这些动作条件。本轮临时短信、电话、上级通知设置已按版本恢复，运维临时通道已禁用；历史通知事实不回退。
- 本机后端续验时启用了 `app.advisory.auto-voice.enabled`、`app.rule-engine.c04.enabled`、`app.disposal.receipt-sync.enabled`。这不是生产默认配置变更；设备回码不代表射频发射，短信和电话未实际发送/拨打，未接入实拍视频。
- 14 项模拟器测试、18 项后端受影响测试、隔离 PostgreSQL SQL 验证通过。告警/交接页面、刷新和会话过期后重新登录均回读同一事件。详细关联与边界见 [全流程模拟验收](../../docs/交付/信号模拟器五组样本-20260923/全流程模拟验收.md)。

## 地图输入与通知收件箱（2026-09-24）

- 打开 `http://127.0.0.1:8766/`，地图上方“计划与气象输入”中切换飞行计划、气象预报，右侧展开系统输入表单。切换页签或收起右侧时保留当前页面内草稿，页面刷新后重新生成；不会自动提交。地图侧栏通过布局分栏避让，切换时重新适配场景范围。
- 输入表单使用系统已有航线版本和计划；地图仍为当前本地 MQTT 场景，不把系统选择的航线或预报区域伪画成本地场景。计划样本按航线有效期裁剪；气象样本按计划时段生成，历史时段保持历史，不冒充当前有效。编号、JSON 修改与幂等语义保持原实现。
- 顶部“通知收件箱”打开 `/external.html`，分飞手短信、飞手电话、风险通知、处罚通知。显示实际保存的模拟内容、接收对象和时间；正文完整换行，原始记录按需展开。“已收到”仅计入有明确送达或模拟播放完成记录的消息；其他发送状态单独显示，签收不替代送达。
- 风险与处罚直接从现有 `/handoffs`、详情及各次 `/deliveries` 读取，不再需要前端绑定事件。每页分别读取最多 20 条 mock 和 20 条 replay 事项，保留每次发送记录并支持翻页；前端数量仅对应当前页。
- 短信和电话读取最近 100 个当前账号可见的模拟告警下 `/advisory` 的真实记录，仅展示 `simulated=true` 且渠道类型明确的记录。范围在页面显示；没有历史记录时不根据当前配置补造。电话展示录音通知文字及已保存播放状态，本轮不提供音频播放。
- 接口读取失败、无权限或部分读取失败明确提示，不显示成“全部没有消息”。后台仍按原权限裁剪记录。未连接时先“登录系统”；地图、计划与气象输入、四类收件箱共用同一个系统登录；凭据仅存在本机服务内存，刷新或切页无需再登录，服务重启后登录一次。
- 新增本机只读 `GET /api/external/inbox?kind=sms|voice|risk|punishment&page=1` 聚合已有平台 GET 接口；不新增平台后端、写接口或通知渠道。旧绑定与回执接口保留兼容，但本页没有绑定、发送、拨号、回执按钮，也不会后台自动调用。既有绑定和历史不删除。
- 只读刷新间隔 15 秒，页面隐藏或地图内输入面板收起时停止对应刷新；正在读取时不重复发起。收到 401 会清空连接和旧记录、保留输入草稿。

布局参考 [QGroundControl Plan View](https://docs.qgroundcontrol.com/master/en/qgc-user-guide/plan_view/plan_view.html) 的地图、对象与参数分区，以及 [Foxglove Layouts](https://docs.foxglove.dev/docs/visualization/layouts) 的任务页签组织。沿用本项目深宝蓝、青蓝强调色、底图与图标资源，不复制其飞控或数据能力。

本节替代此前外部接口页的四类输入/输出混排、人工绑定与模拟回执操作说明；旧记录与相应后端接口未移除。

本轮验收：业务前台 build、scan、admin-migration（1 项）及 git diff --check 通过；模拟器 Python 17 项、前端契约与会话 14 项、通知聚合隔离 8 项及翻页并发检查通过。浏览器检查地图分组、风险展开收起、嵌入输入草稿保留、四类通知的隔离展示与窄屏布局；本机四类收件箱均已读取既有模拟记录，包括短信 13 条和电话播放完成 3 条（验收时当前范围）。本轮未运行新的 MQTT 批次，未发送真实短信或拨号。

## 通知后的目标行为与预报表单（2026-09-24）

- 地图“设备与目标”选中无人机，在右侧“通知后的飞行行为”选择短信后撤离、电话后撤离、继续原轨迹或短信后停止目标上报。默认不联动，旧场景原轨迹不变。选择撤离时在地图绘制至少一个撤离航点，填写速度并应用设置；从通知触发时实际位置连续飞往这些航点，末点停留。当前图示位置到末点的时间仅为几何估计，超过 10 秒会提示；末点不代表已越过系统告警空域。
- 点击启动沿用现有本机 replay MQTT 链路和前置资料，不自动核实告警、不修改通知渠道、执行飞手或反制权限。是否通知由原后端流程决定。继续原轨迹能否保持在告警空域取决于所绘路线；停止目标上报同时作用于主、辅助来源，设备心跳继续。
- 通知监视在本机运行任务中进行，与浏览器、收件箱是否打开无关。用本批唯一无人机序列号严格关联 replay 目标，再锁定本次创建的唯一告警；关联不唯一、读取失败、历史/真实/失败通知不触发。只接受同一告警的模拟短信送达/电话播放完成记录，每目标只执行一次行为，不重复回到起点。动作采用实际读取后的触发时间，不回填轨迹或伪造及时响应；暂停恢复后仍用当时实际位置起步。
- 运行中可选择对象、切换页签、缩放查看；场景编辑禁用。右侧顶部展示该次通知观察，含系统阶段、短信/电话事实时间、最新位置时间、观察截止与后端结论。无新位置、观察读取失败、权限不足保持未知，不以停止上报或通知成功冒充飞离。运行结束保留最后读数及时间，服务重启只恢复历史，不续发。
- 后端新增只读 `GET /api/v1/uav-events/{eventId}/advisory/observation`，复用原 `PilotDepartureWatch` 与 10 秒窗口；返回 `channel`（SMS/VOICE）、`status`（NOT_STARTED/WATCHING/ASSESSED）、`presence`（LEFT/STILL_PRESENT/UNKNOWN）、`started_at/deadline_at/evaluated_at`（毫秒）。缺少时间不补造，只有 ASSESSED 才展示确定结论。权限与数据范围沿用告警读取，不写状态、不触发通知。旧服务尚无接口时模拟器显示观察结果不可用。
- 气象预报输入改为表单：选择计划、预报区域、发布时间、多段预报及气象数值；界面时间明确为北京时间，接口仍为 epoch 毫秒。与折叠的“接口报文”双向同步，切换页签保留草稿；过期、无时间覆盖显示提示。提交校验沿用现有接口的数值范围、整数项、发布时间后 7 天、有序不重叠时段。计划切换后请核对所有预报时段；服务端幂等和版本事实不变。
- 本轮验证：模拟器原 18 项 Python、14 项前端契约、8 项响应隔离用例、天气时区/同步/校验检查通过；后端观察与通知回归 22 项通过；业务前台 build、scan、admin-migration 通过。浏览器用隔离数据验证绘制撤离路线、草稿保留、运行中查看且不可编辑、飞离结论展示。没有新增 MQTT 批次或提交天气数据，没有发送短信或拨号；完整实时“送达→轨迹→飞离”还需运行获准的本地模拟场景验收。


## 统一登录（2026-09-24）

- 任一页面「登录系统」成功后，共用同一账号和系统 API；其他已打开页面自动同步。地图顶部已登录后显示「系统连接」，可按需配置 MQTT 或切换账号。
- 登录不读取 MQTT 配置、不启动发送，也不提交计划或天气。启动模拟时按原规则读取本机 replay Broker；没有配置或没有权限会如实报错，收件箱和输入接口的登录不受此影响。
- MQTT 运行、暂停或准备过程中，服务端禁止切换登录；切换成功后清除旧 Broker 与 MQTT 凭据，重新按新账号权限读取。失败登录保留原会话。
- 后端返回 401 时所有页面共享的登录失效，运行中的发送请求停止；页面清除旧业务读取结果，保留输入草稿。延迟返回的旧会话 401 不会清除新会话。
- 密码、令牌不返回页面、不写入场景和运行文件；服务重启后仍需重新登录一次。

本次验证：共享会话专项 9 项、既有 Python 18 项及前端契约/会话 14 项通过；另核对未登录页面自动发现登录、换账号清除旧读取、失效保留草稿。隔离浏览器验证地图登录后直接进入嵌入计划输入及收件箱；实际 8766 已重启加载，旧批次仍为 COMPLETED / 1437 条确认。本轮未使用真实账号登录验收，也未启动 MQTT 或发送通知。


## 浅色工作台（2026-09-24）

实际入口仍为 `http://127.0.0.1:8766/`。信号地图、通知收件箱、嵌入的计划/气象表单、登录与运行记录统一使用浅色界面；静态文件刷新即可生效。

- 左侧选择场景对象，中间切换设备目标、航线区域、计划气象，右侧编辑当前对象或输入表单。
- 顶部「场景工具」收纳既有载入样本、恢复草稿、导入和导出；「运行记录」打开原有系统回读与发送记录抽屉。
- 底部「开始模拟」沿用原 MQTT 启动逻辑，运行期间保留暂停与停止；历史批次与草稿继续使用原存储。
- `web/light.css` 仅作用于模拟器，使用同一真实地图资源的浅色主题；不改变实际坐标、发送位置、地图资源或业务前台配色。
- 设计稿中的静态样例、流程进度未作为真实运行结果搬入。通知仍为只读模拟收件箱；未登录、未收到、失败和未知继续显示实际状态。

本次为界面改造，验证时不启动 MQTT、不生成业务数据、不发送通知或操作设备。已检查桌面和窄屏地图、工作区切换、运行记录和未登录收件箱/表单；未登录状态下未实测已登录的通知明细及表单提交。


### 登录后补充验收（2026-09-24）

通过现有本地测试账号在实际 8766 页面登录。地图、通知收件箱、嵌入计划/气象输入自动共用登录，刷新地图后仍显示已登录。读取到短信已收到 13 条、电话已收到 3 条；风险当前页已收到 16 条及其他记录 8 条；处罚当前页已收到 9 条及其他记录 9 条。以上为本次读取范围，非全库总量；均为已有模拟记录。计划输入能读取航线和历史受理记录，气象输入能读取计划、表单及历史受理记录，浏览器未记录错误。

此次仅登录和读取，未提交计划/天气、启动 MQTT、发送通知或操作设备。此前“已登录明细未验证”的界面读取边界已补验；提交及实时发送仍不属于本轮验证。
