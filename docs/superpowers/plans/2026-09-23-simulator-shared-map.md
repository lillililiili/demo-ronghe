# 信号模拟器共用系统地图实施计划

用户已确认：模拟器地图与当前系统一致，设备、航线、区域在两边按相同经纬度定位。

## 实施边界

- 复用业务前台 `offlineMap.js`、`mapTheme.js`、地图 CSS tokens 和已安装的 MapLibre/PMTiles；不新装依赖，不复制地图包。
- 模拟器独立构建地图入口，通过限定的本机地图资源代理读取系统当前配置和数据包，保留 Range 206。
- 原场景内部坐标继续按原 WGS84 换算保存，渲染使用真实地图投影；新选点不再限制在旧示意图矩形。
- 地图平移、缩放和选中定位不改场景；绘制、坐标输入、设备组放置才修改草稿。地图失败提示与 MQTT 状态独立。
- 不发送新模拟批次、不修改平台历史，不覆盖本地草稿；保留 MQTT 协议、风险和动作边界。

## 执行

- [x] 增加共享地图构建入口、真实地图投影适配和本机地图资源代理。
- [x] 替换示意底图，将对象绘制和选点转换为 WGS84；支持全览、定位、平移与缩放。
- [x] 调整坐标校验以支持旧画布外的真实地点，并验证旧场景坐标不变、MQTT 报文坐标一致、非法坐标被拒绝。
- [x] 更新使用文档；构建共享入口、业务前台，执行现有必要检查。
- [x] 重启模拟器，在浏览器验证实际地图、旧对象位置、设备组与绘制功能，不启动 MQTT 发送。

## 验证结果（2026-09-23）

- 共享地图入口和业务前台构建通过；8 项模拟引擎测试通过；scan 和旧管理入口 Playwright 回归通过；Python/JS 语法与 git diff --check 通过。
- 系统与模拟器地图配置、清单 SHA256 相同；PMTiles 头请求返回 HTTP 206 和准确 Content-Range。路径穿越请求返回 400，无 Range 整包请求返回 416。
- 8767 隔离页面完成五类设备整组放置、缩放、平移、计划绘制、区域绘制与保存重载；设备经纬度保持不变。未连接 MQTT 或业务库。
- 8766 原页面已刷新为真实底图，原草稿的 1 个设备组、1 个目标、1 条计划、2 个区域保留；地图画布只有 1 个，浏览器无错误；1440 视口文档宽度为 1440。
- 原批次 sim-0922055546-fec1 仍为 COMPLETED，Broker 确认数仍为 103，未重新发送。地图服务默认依赖 localhost:5173；现场设备与真实通道未验收。
