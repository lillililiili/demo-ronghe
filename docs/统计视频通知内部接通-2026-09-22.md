# 统计、视频、通知内部接通交付记录

日期：2026-09-22；按用户确认计划实施，两仓库均本地 main，原有未提交改动逐文件与开工备份对比保留。

## 页面变化及交付结论

| 范围 | 现在看到什么、怎么操作 | 结论 |
| --- | --- | --- |
| 运行统计 | “新增目标数”按首次发现归属并去重；非法/高风险按生成时最新状态；案件按立案；设备为权限范围当前快照。时长/里程等无可靠依据时保留位置显示暂不可统计及原因。显示生成时间和模拟/回放来源，导出同源 | 业务数据统计已接通 |
| 告警、反制办理、合法性研判及融合感知 | 原“查看视频”与光电视频入口共用后端目标查询。明确模拟任务及匹配成功回执才展示模拟画面，持续标注非设备实拍。切目标、关闭、离页、失权或失败清理旧画面 | 模拟视频内部链路通过 |
| 飞手短信/电话、通知上级、计划反馈、处罚部门 | 沿用原触发和接收关系；短信 UNKNOWN、计划/风险送达待确认与结果未知如实显示，不盲目重复投递。后台诊断实际任务开关和 WAV 文件。历史成功不被停用配置覆盖，送达/接通/播放/签收独立 | 模拟通知闭环通过 |

这是代码、构建和隔离验证交付：运行中的开发后端未重启，开发库未应用本次迁移、未开启停用通道；更新后端并正常应用 V202609220001 后才能在原运行环境看到完整新链路。真实现场视频、正式短信、电话录音和真实单位通道仍未接入。没有自动处罚移送、外部计划/天气或真实设备反制扩展。

## 验证证据

- 后端最终联合回归 **140/140**，0 失败、0 错误、0 跳过：统计11、后台业务报表24、光电/视频4、短信27、电话20、WAV校验3、通知目录12、处罚通知9、计划核验反馈5、原通知上级/交接25。
- PostgreSQL/PostGIS **92/92**，0 失败、0 错误、0 跳过：统计11、视频4、短信29、电话21、目录12、计划5、处罚通知10。包含真实 SQL、事务、并发防重、追加迁移、历史任务/记录及原约束保护；处罚测试按原有独立库前缀单独执行，最初不同前缀的跳过已补跑通过。
- 后端 `bash ./mvnw -q -DskipTests package` 成功。
- 管理端 `npm run lint`、**86/86** Vitest、`npm run build` 成功。
- 业务前台构建与 `node tools/scan.cjs` 通过，三个保留测试文件合计 **5/5**。
- Playwright **2/2**：统计缺失原因/真实零值/回放来源可见；六个旧管理入口迁移提示、不自动跳转。项目 npx 路径未解析出 playwright，使用同版本本地 `node node_modules/@playwright/test/cli.js test ...` 成功。统计浏览器用例为明确接口夹具，真实后端聚合另由 PostgreSQL 接口用例验证。
- 构建仍有既有大包体积提示，无编译错误。没有用功能测试宣称真实流、真实送达或算法统计准确率已验收。
- 独立代码复查发现一次导出只读事务与审计冲突，已改为导出方法可写 REPEATABLE_READ，并经 PostgreSQL CSV/XLSX 验证。
- 两仓库 `git diff --check` 通过；没有修改旧迁移、旧样本表或开发通道。重启恢复通过持久化在途任务和过期租约验证，不是操作系统强杀试验。

隔离数据库：`advisory_verify_connectivity_20260922` 使用随机 schema 并按类删除；处罚通知使用独立 `handoff_notice_verify_connectivity_20260922`。测试运行结束即停止，无真实外呼。WAV 测试夹具不含正式业务录音。

本机执行日志与结果保存在 `/tmp/dongying-connectivity-20260922/`：`backend-final-tests.log`、`backend-results.json`、`backend-package.log`、`pg-combined.log`、`sms-disabled-postgres.log`、`pg-handoff.log`、`front-build-final.log`、`front-scan-final.log`、`admin-{lint,test,build}.log`、`front-e2e-final.log`；同时保留每项红→绿测试证据。

## 代码变更说明

前台文件相对 `dongying-vue/`，后端/管理文件相对同级 `houtaiguanli/`。

| 文件/类 | 方法/函数 | 方法作用及本次变更 |
| --- | --- | --- |
| `ReportingService.java` | operations、allowed、metric、类型标签/聚合辅助函数、toCsv、resolvedReport、exportCsv/exportExcel、响应 records | 聚合真实业务事实、源权限、可用性/生成时间；可写导出审计及一致快照 |
| `ReportingRepository.java` | targets、cases、where、params、TargetFact/CaseFact | 首次发现/立案时间范围和有效元组过滤；有效处罚结果查询 |
| `OperationsWorkbookWriter.java` | summarySheet、distributionRows、regionSheet、metric、number、rate | 说明不可用原因，未知不补零，金额保留分，设备当前范围 |
| `src/pages/StatsPage.vue`、`src/services/statsApi.js` | mapOperations、named、kpiList、rankHtml、regionTable、drawCharts、metric helpers、unmount | 空值保留、可见统计口径、图表缺失提示、来源时间和图表释放 |
| `ruoyi-ui/src/api/reports.js` | normalizeReportPreview、named | 后台旧消费者同步保留 null、availability 与生成时间 |
| `TargetVideoController.java`、`TargetVideoService.java` | GET /targets/{id}/video、video、validReceipt、result | 只读统一视频查询，目标/设备权限及模拟回执匹配 |
| `EoEdgeRepository.java` | latestTaskByTarget | 稳定选择当前/最新任务，不写入设备指令 |
| `src/services/deviceApi.js`、`src/components/video/targetVideoState.js`、`TargetLiveVideo.vue` | targetVideo、targetVideoState、load/reset/watch/unmount | 单接口读取、模拟播放门槛、取消与迟到响应保护 |
| `src/pages/SituationPage.vue` | onTipAction、selection watch、unmount | 光电视频占位改共享弹窗，切对象/离页关闭 |
| `AutoSmsService.java`、`AutoSmsRepository.java` | poll、process、claim、finish、overview、sendingCandidates | 未知与明确失败分开；停用时只核对旧发送，不重投 |
| `NotificationDirectoryService.java` | diagnostics、deliver、unknownDelivery、构造器 | 实际任务/录音诊断；适配器异常保留未知事实 |
| `V202609220001__automatic_sms_unknown_result.java` | migrate | 追加 UNKNOWN 状态约束，保护历史及送达记录约束 |
| `src/components/disposal/autoSmsView.js`、`AutoSmsNotice.vue` | autoSmsView、compactTitle | 未知文案、提醒样式、禁止补发 |
| `src/pages/flights/components/PlanVerificationPanel.vue` | notificationBlocker、notificationMessage、模板 | 计划反馈未知结果不冒充未发送 |
| `src/pages/FlightsPage.vue`、`src/pages/airspace/AirspaceRiskEventDetail.vue`、`src/ui/labels.js` | 通知状态/原因映射，无新增业务方法 | 送达待确认及结果未知文案，原有页面工作保留 |
| 前台 `tools/statsApi.test.cjs`、`targetVideoContract.test.cjs`、`notificationConnectivity.test.cjs`、`e2e/stats-connectivity.spec.js`；后台 `tests/reportsApi.test.js` | 测试函数 | 缺失与零、视频生命周期、未知通知和实际页面展示回归 |
| `ReportingApiTest`、`OperationsPostgresTest`、`EoManualTrackApiTest`、`TargetVideoPostgresTest` | API/隔离数据库测试 | 统计口径、范围、去重、金额及视频回执/权限 |
| `AutoSmsApiTest`、`AutoSmsPostgresTest`、`AutoVoiceApiTest`、`DirectoryApiTest`、`DirectoryConnectivityPostgresTest`、`FlightNotificationPostgresTest` | 通知/迁移/隔离数据库测试 | 双渠道独立、未知重试限制、停用恢复、历史冻结与目录/反馈链路 |
| 两仓库 README；后端 `docs/运行统计接口契约.md`、`docs/目标视频查询接口契约.md`、`docs/模拟通知内部闭环-2026-09-22.md` | 无具体方法 | 更新页面入口、契约、迁移与真实接入边界 |

按统计、视频、通知分别在两个仓库提交；工作区仍保留开工前的合法性、飞行计划、天气、地图等其他任务改动，不把它们纳入本批提交。
