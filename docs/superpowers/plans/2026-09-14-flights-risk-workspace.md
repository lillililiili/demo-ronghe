# 飞行计划与风险工作区实施计划

目标：落实本对话已确认的“左侧列表—中间地图—右侧详情”，使用说人话的文案。飞行计划参考司空 2 的任务组织，风险参考 FlytBase 的事件详情，空域与气象位置沿用当前可信几何。

## 边界

- 在 main 修改，保留其他会话改动；不新增依赖，不修改后台业务状态和审批规则。
- 用户已确认布局并要求继续，不重复请求批准。
- 用户要求不测试：不运行测试、构建、浏览器验收；仅阅读代码和差异，执行 git diff --check。
- 缺少位置、关联计划、权限或接口返回时显示真实缺失原因，不补造事实。

## 实施步骤

- [x] 列表：新增本页专用 FlightRecordList.vue 与 FlightListPager.vue，以卡片显示名称、状态、时段和关键摘要；选中事件只输出记录 ID，由父页面读取详情。
- [x] 布局：FlightsPage.vue 两个页签均采用列表、地图、详情三个区域；宽屏并排，小屏将地图和详情分行，局部滚动。
- [x] 地图：保留一个 MapView、故障设备图标和气象范围层；当前选中记录与地图共享对象，地图缺失不显示上一条位置。
- [x] 导航：计划跳风险携带 risk 与 plan；风险跳计划携带 plan；筛选范围可见并可清除，链接刷新后仍定位相同记录。
- [x] 详情：关联计划入口、风险依据、核验和通知记录集中展示；动作复用当前权限、状态和 API，不添加新的写操作。
- [x] 差异：核对选择、加载失败、无数据、无位置、无权限及离开页面的清理路径，更新 README，检查本次差异。

## 模块关联

| 来源 | 传递 | 接收方 | 结果 |
| --- | --- | --- | --- |
| 计划列表 | plan_id | 计划详情、航线图、设备检查 | 读取同一计划的执行情况与点位 |
| 计划风险记录 | risk_id、plan_id | 风险列表、详情、地图 | 显示该计划的风险并选中指定风险 |
| 风险详情 | plan_id | 计划列表与详情 | 精确读取关联计划，不以列表首行替代 |
| 风险筛选 | 类型、等级、状态、区县、计划 | 服务端风险列表 | 分页、排序及 CSV 沿用统一查询 |
| 地图故障设备、检查记录 | device_id | 设备监测 | 沿用现有精确设备详情链接 |

本次只调整展示、选择和导航，不改变风险是否成立、是否允许核验、是否可以通知的判断，不新增 AI 能力。

## 完成记录

已完成代码和文档调整、阅读差异及空白检查；未运行测试、构建、浏览器或视觉验收。当前工作区的其他气象图层改动予以保留，不计为本次新增能力。
