# 内部依据：操作说明（值班员）

> 屏幕文字于 2026-09-10 按当时的页面重新逐字核对过一遍。

不随交付件外发。锚点用「文件 + 方法名/常量名」，不用行号。

| 正文里的说法 | 出处 |
| --- | --- |
| 需要改密的账号登录后先跳修改密码页 | `dongying-vue/src/services/auth.js` 的 `needsPasswordChange`；`dongying-vue/src/router/index.js` 的 `beforeEach` |
| 工作台顶部五个方块（全部事项、高等级事项、无人机告警、飞行计划风险、设备告警） | `dongying-vue/src/pages/WorkbenchPage.vue` 模板的 `.wb-kpis` 一段 |
| 每张卡片一行"下一步"，灰按钮旁写原因 | `dongying-vue/src/services/workbenchEvents.js` 的 `nextStep`；页面渲染在 `WorkbenchPage.vue` 的事项卡片模板 |
| 计数读不到时显示"无读取权限""设备归属映射未配置"，不显示 0 | `workbenchEvents.js` 的 `AVAILABILITY_LABEL` 与 `workbenchStats` |
| 清单按等级与接收时间排序 | `WorkbenchPage.vue` 列表区的排序说明与服务端排序 |
| 悬浮卡三行、缺项不渲染 | `dongying-vue/src/pages/SituationPage.vue` 的 `tipSummaryRows` |
| 禁飞区的孔是可飞区、不填充 | `dongying-vue/public/assets/js/map.js` 的 `_paintLayers`（even-odd 填充）；`dongying-vue/src/services/situationData.js` 的 `polygonRings` |
| 只报方位的目标画方位线、设备无坐标则不画 | `situationData.js` 的 `bearingOrigins` / `attachBearing` |
| 三个核实结论的名称与后果 | `dongying-vue/src/ui/uavVerificationModal.js` 的 `conclusion` 字段 options |
| "属实只表示已核实、待处置"、无核实权限时的提示 | `workbenchEvents.js` 的 `nextStep`（无人机事件分支） |
| 没有证据查看权限时证据链的提示，且不发该请求 | `dongying-vue/src/pages/AlarmsPage.vue` 的 `loadChain` |
| 反制申请需另一人批准 | `workbenchEvents.js` 的 `nextStep`；`server/.../modules/disposal/domain/DisposalRules.java` 的 `requireTwoPerson` |
| 审批弹窗的批准/驳回、驳回必填意见、批准后显示有效期与审批人 | `dongying-vue/src/ui/disposalAuthModal.js` 的 `openDisposalApproval` |
| 执行等设备回执、人工通道改为"登记执行结果"、执行受阻如实说明 | `disposalAuthModal.js` 的 `openDisposalExecution` / `openDisposalManualResult` |
| 停止会撤销授权并尝试急停；设备不支持急停时授权仍停止（"授权停了"≠"设备真停了"） | `disposalAuthModal.js` 的 `openDisposalStop`；`DisposalRules.java` 的 `deviceStopResult` |
| 动作按钮由服务端逐条下发，不该点的不显示 | `DisposalRules.java` 的 `allowedActions`；`dongying-vue/src/pages/PunishPage.vue` 的 `DISPOSAL_ROW_ACTIONS` / `disposalRowActions` |
| 交接"提交成功只表示材料入库，不等于已送达" | `workbenchEvents.js` 的 `nextStep`（风险通知上级分支） |
| 处罚办理的按钮与顺序，以及撤案、作废、新增待补线索、标记已补齐、预览全文、复制全文 | `PunishPage.vue` 模板中的按钮文案；弹窗在 `dongying-vue/src/ui/punishmentModals.js` |
| 复核人不能是承办人 | `server/.../modules/punishment/domain/PunishmentRules.java` 的 `requireDifferentReviewer` |
| "案件只在本平台内办理，不会自动报送外部处罚系统"，以及决定书、复核结论两条同类说明 | `PunishPage.vue` 的 `BLOCK_GAPS`（`case` / `doc` / `review`） |
| "驱鸟处置"按钮禁用、提示"设备指令与作业参数待设备方确认" | `dongying-vue/src/pages/spacerisk/SpaceRiskPage.vue` 详情动作区与 `scareNotAvailable` |
| 重启原因必填、"模拟回执不代表真实设备已重启"、离线或协议不支持时失败 | `WorkbenchPage.vue` 的 `openDeviceReboot` |
| 恢复校验通过/未通过的提示 | `WorkbenchPage.vue` 的 `runDeviceAction` |
| 设备实时监测的两条读取失败提示 | `dongying-vue/src/pages/MonitorPage.vue` |
| 无权访问页的文案 | `dongying-vue/src/pages/AccessDeniedPage.vue` |
| 复核岗的六项菜单 | 2026-09-09 实测 `/auth/me`（reviewer1） |
| 区域筛选读不到时只剩"不可用" | `AlarmsPage.vue` 的 `regionOpts` / `paintRegionOptions` |
| 导出只有告警与风险两处，飞行计划列表没有导出入口 | `dongying-vue/src/services/alarmApi.js` 的 `exportAlarmsCsv`、`riskApi.js` 的 `exportRisksCsv`；`FlightsPage.vue` 只在风险页签有 `exportRiskCsv` |
| 审计日志可查操作人、模块、动作、时间 | 2026-09-09 实测审计日志接口返回含账号、角色、模块、动作与时间 |

## 说明

正文里"交班要交代的四件事"是按上述页面能查到的信息组织的，不是来自某条外部规程；如果甲方有自己的交接班规程，以甲方规程为准。
