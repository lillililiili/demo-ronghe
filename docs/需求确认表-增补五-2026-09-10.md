# 需求确认表 · 增补五（2026-09-10）

接续增补三 F6–F7、增补四 F8–F15。本批来自会议纪要与平台现状的逐条比对，**面向业务方与领导的说人话版（含界面截图）另出**：
`~/Documents/Codex/2026-09-10/确认事项书/需求确认事项书-2026-09-10.docx`（同目录有 PDF 与 12 张截图）。

原则：会议纪要《无人机融合感知平台 详细会议纪要 V1.0》（2026-08-23）没写明的，平台按"Demo 先行、正式替换"给一个能跑通的临时口径并在界面标"演示值"，不据此推定业务口径。未答复条目维持现状，后续变更按需求变更处理。

| 编号 | 事项 | 当前实现 | 需要确认 | 不确认的影响 |
| --- | --- | --- | --- | --- |
| F16 | **「通知上级」发给谁、怎么发**。纪要全文没有"上级接口"这一说法；对外只提到接收外部管服平台的飞行计划（入向）、通知机场、公安信号干扰联动的字段要求。 | 点"通知上级"生成交接单入库，状态恒为"待投递"，阻断原因"通知渠道未接通"；接收方为演示数据（公安机关/民航监管部门/演示处罚接收方）；`app.handoff.channel=mock`，不发任何网络请求。 | ① 风险通知与处罚移送各发给哪个单位（全称）；② 接口自动发送还是导出材料人工报送；③ 若走接口，由哪方提供协议与测试环境、何时给；④ 是否一事多送、是否要回执、超时口径。 | 演示只能到"材料已入库"。协议到位后只替换发送通道，页面与流程不改，约 2–3 天。 |
| F17 | **反制与公安信号干扰的联合方式**。纪要 6.3 的人在回路、授权、校验、急停、回执、审计已实现；"联动单位"只是字段；纪要 16.2 已把公安授权流程、授权接口、设备控制边界列为待确认。 | 平台内两人流程（申请人≠审批人）→ 下发指令到我方反制设备 → 执行中/完成/失败/停止/急停 + 设备回执 + 全程审计。不通知任何外部单位。 | ① 公安登录本平台操作，还是平台请求发到公安系统；② 授权在平台批还是外部批完登记编号；③ 谁有权批、对应岗位；④ 干扰设备由谁控制。 | 若实为公安控设备，该段改为"申请—登记—回执"约 2 天；若要对接公安系统，工期取决于对方接口。 |
| F18 | **事项如何分给人**（告警/风险/设备三类）。纪要未涉及值班人数、班次、组长、催办、转派。 | 共享待办池：同一工作台按等级与接收时间排序，谁先点谁处理。无分派/认领/催办/转派/超时提醒。仅处罚案件可"指派承办人"，处置授权强制两人。 | ① 值班人数与班次；② 自动分派/抢单/组长指派；③ 是否要催办、转派、超时提醒及超时口径；④ 组长角色与可见范围；⑤ 申请人/审批人对应岗位。 | 演示看不出人员分工。分派+认领约 2 天，催办与超时提醒约 1 天，转派约半天。 |
| F19 | **角色设置**。 | 仅"超级管理员"与演示"复核员"两个角色；权限按页面与动作两级配置；用户仅 2 个。 | 正式上线的角色清单（值班员、班长、审批人、设备运维、法制审核、只读领导等）及各自页面与动作权限。 | 演示只能用超管账号，体现不出两人审批与分级查看。确认后当天可配置完成。 |
| F20 | **设备调测手段**。纪要 8.1 只要求协议资料含心跳、状态机、错误码、超时重试、控制类回执与急停，未列调测手段清单。 | 调测任务五步（创建/连接/保存配置/执行/报告）；监测页仅"远程重启"与"恢复校验"。 | ① 现场与运维实际需要的操作（自检、校时、参数下发、固件升级、日志回传、通道自环、标校等）；② 设备方能否提供对应指令与回执（**需设备/算法方答**）；③ 各操作是否需审批。 | 现只有重启一种控制手段。每增一种约半天到一天，前提是设备方先给指令与回执定义。 |
| F21 | **实时监测页面模块**。原型上的"工作状态、探测距离、最大高度、覆盖角度、工作频段"中，仅工作状态与雷达探测距离/覆盖角在协议 A 有来源；其余无来源，原型为假数据。 | 显示连接状态、健康、最后心跳、时延/丢包/信号、指标曲线、活动告警、设备事件流。 | ① 值班最需关注项；② 能力参数是否人工维护、由谁维护（同 F15）；③ 是否需要跨设备滚动日志流。 | 页面比原型少几块。补齐版式约一天；能力参数需先定维护责任。 |
| F22 | **四条主流程逐条确认**：飞行合法性、空间安全风险、反制处置、处罚办理。 | 见确认事项书第五节，各流程环节已实现并可演示。 | 每条：环节增减、每步责任岗位；另含"非法是否必须人工复核后才对外""异常与非法处理是否相同""紧急能否先执行后补批""急停由谁按""决定书由平台出还是外部系统出"。 | 流程与实际办案习惯不符会在验收时集中暴露，改动量取决于差异。 |

## 仍未答复的旧条目

F6（航线风险时间范围与邻近距离）、F7（执行中计划旁的风险是否驱离）、F11（平台编号格式）、F14（判定规则二十余项演示参数）、F15（设备能力参数）。
F9、F10（去掉"已批准"状态、执行状态按时段推进）已按建议实施，**请追认**。

## 对方尚未到位的资料（按纪要第十五节行动项）

| 行动项 | 内容 | 计划时间 | 目前状态 |
| --- | --- | --- | --- |
| A2 | 雷达、光电、云台、反制、5G-A、TDOA 原始协议 | 08-24 | 已收到凌云 MQTT、雷达 TCP、四通道反制 TCP |
| — | 上级通知/移送接口协议 | 纪要未列 | 未收到，纪要中亦未定义（见 F16） |
| A3 | 光电识别与跟踪、雷达正式轨迹算法 | 08-24~25 | 未收到，平台用演示算法占位 |
| A3 | 多源融合可信度正式权重与降级策略 | 08-24~25 | 未收到，平台用可配置演示权重 |
| A8 | 公安信号干扰授权接口与验收环境 | 08-31~09-01 | 未收到，纪要 16.2 已列待确认 |
| — | 飞手位置（TDOA / 5G-A） | — | 未接入，"超视距"一项恒为不可判定 |
