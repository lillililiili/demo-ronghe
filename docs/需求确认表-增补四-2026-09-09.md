# 需求确认表 · 增补四（2026-09-09）

接续《需求确认表-增补三（2026-09-08）》F6–F7。本条来自飞行计划页"外部授权登记"区块的评审，属于功能取舍，不是缺陷。

| 编号 | 事项 | 当前实现 | 需要客户确认 | 不确认的影响 |
| --- | --- | --- | --- | --- |
| F8 | **外部授权登记是否保留**：飞行计划详情页的"外部授权登记"区块，用于把别的单位（空管、公安等）在平台之外批下来的飞行许可文号记到这条计划上，作为研判依据。 | **已整体撤除（2026-09-09）**。阶段 9 曾实现（决策 9-10）：登记为只增记录，不改变计划状态，同一计划同一文号只登记一次，需要单独权限，登记同事务写审计；合法性研判从未读取这条记录。撤除范围：计划详情页的区块与按钮（提交 4d10546）、登记与查询接口、"计划与实际对照"汇总里的授权段、权限目录中的"登记飞行授权"及各角色上的授予、登记表（迁移 `V202609090106`）。该表从未登记过真实文号，删表不丢业务事实。 | 1）客户业务里是否存在"平台外批文、平台内补登记"的场景；2）若存在，由谁登记，批文单位、文号、有效期三个字段是否够用；3）登记之后是否要影响合法性研判结论。 | 已按"不保留"处理，客户不答复也不影响演示。若客户日后答复要保留：需重建登记表与接口、恢复页面区块，约 1 天；若还要影响研判，规则引擎再加一条"存在有效外部授权"输入，合计约 2 至 3 天。 |

补充说明：

- 撤除不影响其他模块：飞行计划的状态、对照、风险与合法性研判都不依赖这条记录。
- 若客户日后答复要保留，建议同时确认外部批文的来源清单（哪些单位的批文算数），否则登记内容无法核验。
| F9 | **计划状态里的"已批准"由谁批**：计划列表与详情把状态码 APPROVED 显示为"已批准"，与"待执行"并列。 | **已按"不区分"处理（2026-09-10，迁移 V202609100001）**：APPROVED 并入 PENDING，页面与筛选只剩待执行 / 执行中 / 已完成 / 已取消。平台本身没有审批动作，也没有审批单位、审批时间字段；状态码原样来自报备来源（导入或模拟源写入什么就显示什么）。合法性研判是对感知目标做的，不改计划状态，"已批准"不是研判之后的状态。原型里每条计划都带"审批单位 / 审批时间 / 审批结论"，状态只有待执行、执行中、已完成、已终止，没有"已批准"这一档。 | 1）报备来源送过来的计划是否都已经过外部审批，"待执行"与"已批准"是否需要区分；2）如需展示审批信息，审批单位、审批时间、批文号从哪个来源取。 | 演示时两档并存会被追问"谁批的"。若客户答复不区分：把 APPROVED 与 PENDING 合并显示为"待执行"，半天；若要展示审批信息：报备接口加三个字段并落库，约 1 天。 |
| F10 | **执行状态不随时间流转**：计划时段已过去数天的计划仍显示"已批准"，引擎却已把它匹配到目标；今天窗口内的计划也不会自动变成"执行中"。 | **已按"按时段自动推进"处理（2026-09-10，FlightPlanStatusAdvanceJob）**：未到起飞时刻为待执行，进入时段为执行中，时段结束为已完成，已取消不动；默认关闭，本地演示配置打开。此前状态码只在写入时确定，平台没有按计划时段推进状态的作业；原型的执行状态是按当前时间与计划时段推算的（未到为待执行、窗口内为执行中、过期为已完成/已终止）。页面已改为：引擎有对照结论就照实显示，不再以状态码决定显示与否。 | 1）执行中 / 已完成由谁判定：按时段自动推进，还是等报备来源回传实际起降；2）计划时段已过但从未匹配到目标，算"已完成"还是"未执行"。 | 若按时段自动推进：后端加一个定时作业按 start_at / end_at 推进状态并留审计，约 1 天；若等来源回传：需来源方提供接口，无法单方面完成。 |
| F11 | **平台自己生成的告警和风险没有编号**：规则引擎生成的告警、空间安全风险在列表、工作台、本航线风险里显示为"—"或"实时"，只有模拟来源带来的记录有"告警-0905-006"这样的编号。 | 平台没有自己的编号规则，屏幕上的编号就是来源方给的编号；引擎生成的记录只有内部标识（不上屏）。演示库里带编号的都是模拟来源写死的。 | 1）平台产生的告警、风险、研判是否要有统一的业务编号，格式是什么（例如 告警-年月日-序号）；2）来源方自带编号时以谁为准。 | 正式接入后所有平台产生的记录都没有编号，无法在电话、文书里指代。补一套编号规则约 1 天：编号表 + 告警入库、空间风险评估、研判三处写入 + 历史记录补号。 |
