# 设计稿检查记录

日期：2026-09-20。范围仅为 `docs/designs/2026-09-20-plan-weather/` 独立设计原型。

- 五个视图均可打开：飞行计划天气、后台接口总览、计划输入、天气预报配置、天气传感器。
- 检查 1280×720、1366×768、1440×1024 共 15 个组合：未发现 document 横向溢出，标题、按钮和字段标签未发现容器裁切。低高度页面通过纵向滚动完整阅读。数据见 `responsive-check.json`。
- 已查看前台预报、后台接口配置、计划输入、预报配置、传感器台账的渲染截图。截图位于 `screenshots/`，为视口截图，较长表单需在交互预览中滚动。
- 已操作：天气预报/现场实测切换、未接入/过期/读取失败/无权限/正常状态切换、计划配置校验、天气测试阻断、模拟返回、天气传感器新增表单打开和关闭。天气状态记录见 `weather-states.json`。
- 本地东营地图可加载，未编造业务航迹。导航和状态图标复用仓库已有 SVG path，Logo 复用已有资源。
- 初检发现地图包使用 ESM 而非全局脚本，已修正模块加载并重新检查，后续页面正常渲染。
- 初检发现导出工具完整长截图存在画布扩张，最终交付改为普通视口截图，未对图片做内容编辑。
- `node --check design.js` 通过。`git diff --check` 通过。
- 没有执行生产 `npm run build`、`node tools/scan.cjs`、迁移 Playwright E2E；本次未改生产前端、后端或其依赖。
- 未进行真实接口连通、协议、设备上报、数据库、通知或部署验收。设计稿中的保存和测试结果不能作为上述能力完成的证据。

基线对照：保留前台深蓝、后台浅色，以及计划三栏与设备台账/右侧预览结构；新增区域是方案设计，非生产页面逐像素复刻。待用户审阅本稿后再确定生产实现。

## 用户要求补充修改标记

- 五个视图默认显示紫色“新增”和橙色“调整”虚线框，块内标出具体变化；页首增加当前页变更摘要。
- 通过顶部按钮可隐藏/显示标记；切换页面继承显示偏好。传感器专用表单等新增弹窗也有标记，原页面基线截图不加框。
- 浏览器检查五页标记数量分别为 7、5、4、4、6，无页面横向溢出；隐藏后可见标记数为 0。新增传感器弹窗标记已确认。
- 1280×720 检查标记文字未裁切，1440×1024 已检查设备管理整体效果。五页主截图已更新为带标记版本，旧 06/07 截图仍为前一轮无标记交互示例。
- 新增 annotations.js：mark / annotatePage / annotateModal / noteLegend 负责区域标记、变更摘要及开关；design.js 的 render / modal 接入标记；design.css 增加评审层样式。仅影响独立设计稿。
- 两个 JavaScript 文件语法检查及 git diff --check 通过；未执行生产构建与后端验收。

## 客户业务范围与操作员信息清理（替代前述初稿范围）

- 根据用户纠正，撤回前台“现场实测”、计划关联站点、观测代表范围、航线网格气象匹配及新增天气风险联动；取消固定“未来 3 天”假定。原设计说明及实测截图保留于 `已撤回初稿/`，不作为当前需求。
- 计划详情移除外部计划编号、来源版本、最近接口接收时间；列表去掉接口输入标签。原有业务计划编号、报备单位和计划来源保留。
- 删除产品区“数据进入哪里”“前台如何显示”、地图设计解释、接收成功含义、与当前操作无关的反制和自动流程解释。
- 天气状态切换、模拟接收/测试返回操作移到评审摘要。产品区保留实际业务内容，新增/调整标记仍可关闭。
- 后台接入记录保留管理员排查需要的外部计划 ID、版本及处理结果；浏览器实际打开记录和测试返回确认仍可用。
- 浏览器核对五页：无“现场实测”“数据进入哪里”“前台如何显示”等已删除内容，无文档横向溢出。检查记录 `scope-cleanup-check.json`；五页及设备表单截图已更新，新增 `08-plan-info-cleaned.png`。
- 根级与前端 AGENTS.md 已写入“不擅自扩展业务”和“按操作角色取舍页面信息”两项用户确认要求；保留文件中其他已有改动。
- 修改方法：design.js 的 weather/flights/interfaces/plan/forecast/devices/sensorForm 与弹窗内容收紧范围；annotations.js 的 annotatePage 把设计控制放到评审层并调整标记范围；design.css 增加 review-controls 样式。仅修改设计稿与规范，未改产品前后台实现。
- JavaScript 语法检查、git diff --check 通过；未执行生产构建、迁移 E2E 或真实设备/接口验收。
