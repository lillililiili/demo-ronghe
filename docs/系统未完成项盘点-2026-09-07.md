# 系统未完成项盘点（2026-09-07，main@0496c96）

三路只读扫描（前端页面/控件、后端桩与开关、文档登记项）的合并结果。旧的《系统开发进度说明-2026-09-05》已过期，以本文为准。

## 一、硬阻塞（不是我们能写代码解决的）

| # | 事项 | 谁给 | 影响 |
| --- | --- | --- | --- |
| 1 | 高度基准（海拔 vs 椭球高） | 客户/厂家 | 凌云与雷达高度只存原始值，合法性中所有高度规则对这些来源"待确认" |
| 2 | 各源精度、objectId 生命周期、光电 className 取值表 | 厂家 | 融合权重与关联口径停在 DEMO |
| 3 | 反制/干扰/驱离的授权条件、审批、急停回执（Q5） | 业务方+设备方 | 处置授权域无法开工，四个页面的反制类按钮全部禁用 |
| 4 | 规则阈值、告警合并、统计口径（Q6） | 业务方 | 生产默认没有 ACTIVE 规则集，不会自动判定 |
| 5 | 外部接口：计划/视频/上级/处罚系统、通知渠道与回执（Q4） | 对接单位 | 交接只到 PENDING_DELIVERY，没有送达 |
| 6 | 真实组织/区域/岗位（Q3）、部署与容量（Q8）、证据保留制度（Q7） | 业务方/运维方 | 生产账号、容量、证据销毁不能上线 |
| 7 | 真实设备联调（P6） | 厂家+A+B | 最大交付缺口；输入物已备（凌云回放文件 180 行） |

## 二、缺整块能力（需要新建后端域）

| 事项 | 现状 | 归属 | 量级 |
| --- | --- | --- | --- |
| 处置授权（反制/干扰授权记录、编号、审批人、时限、设备通道、急停） | 无表、无接口、无权限码；前端只有读 Mock 的死弹窗（假编号 AUTH2026xxxx） | A（设备/协议 B）+ B（授权与审计） | 大 |
| 处罚案件（案件管理、罚款裁量、决定书、定性复核） | 处罚页整块"本期未建设"，处罚交接一律 409（缺反制完成事实） | B | 大 |
| 交接送达与回执 | 只有 PENDING_DELIVERY，没有送达写入路径 | A/B + 对接单位 | 中（等 Q4） |
| 光电抓拍/录像取证、实时视频 | 协议 C 只做跟踪，无图像；证据只能人工上传；大屏视频是 Demo 画布 | A | 中（等厂家能力） |
| 机场通报通道、驱鸟、真实鸟类识别、C04 数量/趋势数据源 | 机场只有骨架与只读数据；C04 数量/趋势恒不可用 | B + 客户 | 中 |
| C07 证据链汇总、证据关联更多对象（处罚/空域/授权） | 未建 | B | 中 |
| 融合自动合并/分裂在管线内触发 | 只记 GATE_AMBIGUOUS，合并/分裂靠人工接口 | B | 中 |

## 三、已建但没接到位（接线类，可自行推进）

| 事项 | 现状 | 归属 | 量级 |
| --- | --- | --- | --- |
| 空域与航线规则页、空间安全风险页 | 两个页面（约 1170 行）已接真实接口，但导航撤回后无入口、无引用 | 用户决定挂回或删除 | 小 |
| 工作台设备处置动作（重启/恢复/关闭） | 只有 toast，无接口；后端只有 reboot 命令 | B（后端）/A（设备） | 小 |
| 飞行监管页 4 个 KPI、告警页 2 个 KPI 显示"尚未接入"；两页的导出、筛选、排序禁用 | 需后端统计/导出接口 | B | 小-中 |
| 合法性 C01 的起降点、飞手/单位维度恒不可判定；身份线索（SN）只在回放场景有 | 等真实数据；代码路径已有 | B + 厂家 | 小 |
| AOA 方位线、分路置信度 | 读接口未透出 `bearing_deg` 与按来源置信度 | B | 小 |
| 目标读接口无风险等级/违规事由/处置状态 | 悬浮卡这三项不渲染 | B | 小 |
| 统计页只有近 30 天全量，无时间/类型/区域切片 | 接口不支持 | A | 小 |
| 405（已映射路径用错方法）落到 500 | 与已修的 404 同类 | B | 小 |
| 空域多边形孔洞不画 | map.js 只画单环 | B | 小 |

## 四、前端死代码与卫生（不影响功能，但影响体积与误判）

- `index.html` 仍加载全部 legacy 脚本（13 个页面 + `mock.js` 350 KB + `search.js` 等），但任何路由都到不了它们；建议只保留 `map.js/charts.js/ui.js/geo.js/video.js`。
- 死文件：`JamAuthModal.vue`、`AlarmNotifyModal.vue`（通报已有真后端 handoffApi）、`services/weather.js`、`hooks/useChart.js`、`services/airportApi.js`（无机场管理页）、`ui/counterAuthModal.js`、`ui/deviceRebootModal.js`、`useCarousel`。运行期真正读 `window.MOCK` 的四处全部不可达。
- 登录页"忘记密码"弹窗明文显示开发账号与密码。
- 大屏"证据管理"卡片硬编码"未建设"；`apis` 路由键悬空，若给角色分配会落到 legacy 页。

## 五、运行与部署事实

- 缺省配置下（无 local profile）：`source-mode=mock`、无规则集、融合摄取/规则引擎/C04 任务全部关闭、无种子——服务能起，但所有评估链路是空的，这是有意的（决策 7-x/8-1/9-16），验收必须带 local profile 或显式开关。
- 生产首次部署必须从全新库开始；已迁过 202609070001 的非 local 库升级需一次性 `ignore-migration-patterns`/`out-of-order`（决策 10-17）；迁移 061 被改过，老库需 `flyway repair`。
- C04 定时任务单实例保护只在进程内，多节点部署需数据库租约。
- 70 余个 PostgreSQL 专项用例靠环境变量门禁，普通 `mvn test` 会静默跳过；空间判定 H2 上根本不跑。

## 六、仓库卫生

- `feature/b-integration` 分支领先 main 9 个提交（09-03 的前端认证、列表壳、统计分页等），未合并也无处置结论；另有 8 条 09-03 的远端 feature 分支与 2 条已完全合入可删的分支。
- 10 个 worktree，其中 `.worktrees/feature-b-page-chrome` 的融合感知页仍读 Mock，合回会盖掉阶段 11。
- 《系统开发进度说明-2026-09-05》已过期，只有其 §7 的九项上线缺口仍成立。
