# AGENTS.md

本仓库只维护 `dongying-vue/` 业务前台，前端细则见 [dongying-vue/AGENTS.md](dongying-vue/AGENTS.md)。平台唯一后端和后台管理端位于同级 `../houtaiguanli`。

## 产品原则：自动研判与快速响应

- 无人机经过监测区域的时间可能很短。以缩短发现到有效响应的时间、减少必须人工操作的次数为目标；常规流程由系统持续研判、按已配置规则与有效授权推进，人工集中处理异常、不确定项和需要授权的关键决策。
- 新增或修改流程时，先说明触发数据、自动判定依据、数据时效、自动动作、人工介入条件和失败后的处理方式。不要默认把人工核实、填表、逐级点击或跨页面流转设为所有事件的必经步骤。
- 区分目标识别、规则研判和动作授权。识别置信度不能直接替代有效授权；反制、干扰和处罚决定继续遵守各自的权限、适用范围、有效期、回执与审计要求。自动化范围应由明确策略约束，不能由前端隐藏按钮或定时器代替后端实现。
- 自动跟踪、数据比对、证据汇集、常规通知和结果监测优先按条件并行推进；信息不足、目标失联、数据过期或执行失败时显式转异常处理，不得当作合法、飞离、风险解除或执行成功。
- 页面优先呈现当前结论、依据与更新时间、系统正在执行的动作，以及确实需要人处理的事项；自动流程不依赖用户停留在页面。实时风险处置与事后移送、案件办理分开计时、分开呈现。
- 本节是用户确认的产品方向，不代表所有自动化能力已经落地；当前实现与优化建议见 [自动研判与快速响应优化评估](docs/自动研判与快速响应优化评估-2026-09-15.md)。
- 飞手通知须同时支持短信与电话录音通知，按渠道分别保存触发、发送/拨打、送达/接通、播放及失败事实；一个渠道成功不能替代另一渠道的回执，也不能直接认定飞离或风险解除。真实通道与录音配置的接入状态如实展示，不以模拟回执冒充真实通知。

- 风险通知统一为“通知上级”，不按单位、区域或风险类型区分接收对象，不增加职责单位选择或分发规则。该约定仅统一风险通知目的地，不改变业务风险范围、数据访问权限、计划报送单位反馈、飞手提醒和处罚移送各自的接收关系。

## 界面文字完整展示

- 界面展示不得用 `…`、`...`、`。。。` 等省略号截断或替代文字。导航、标题、按钮、标签、列表、表格和卡片内容应完整可读；空间不足时调整布局、宽度或换行，不得仅隐藏省略号而继续裁切文字，也不得仅靠悬浮提示补全。具体实现与验收要求见 `dongying-vue/AGENTS.md` 的“文字完整展示（强制）”。

## 无人机轨迹与航线颜色（用户确认）

- 轨迹应反映实际飞行过程：按同一目标、同一轨迹的观测时序绘制，保留转弯、停留和断点；不得跨断点、跨轨迹首尾强连，不得为美观补点、平滑或生成不存在的运动。模拟轨迹也应遵守合理运动与连续性，并明确标记模拟来源。
- 按每段与已匹配计划航线的关系显示：符合规划的已飞段为绿色，偏离规划的已飞段为红色，尚未飞过的计划航线为灰色。不能按整个目标的合法性结论把整条轨迹统一染色。
- 缺少匹配计划、走廊依据或有效观测时，不假定符合或偏离；未知关系单独说明。灰色计划线不代表已经飞过，也不能把“没有收到轨迹”直接解释成“尚未起飞”。
- 合法性判定、飞行计划及其他展示航线对照的页面遵循同一口径。具体实现要求见 `dongying-vue/AGENTS.md` 的“地图航线与轨迹展示”。

## 目录与边界

- npm 命令只在 `dongying-vue/` 运行。
- 不在本仓库新增 Java 后端、运维管理或系统管理页面。
- 后端公共契约变化必须同时核对后台仓库和本业务前台两个消费者。
- 页面菜单访问权限与设备业务动作权限保持解耦；业务页需要设备动作时使用模块权限，不依赖已迁出的设备菜单。
- 保留六个旧哈希地址的迁移提示，不做自动跳转。

## 修改范围与历史保护

- 按用户确认，只在本地 `main` 修改和提交，不新建分支或工作树。历史计划中的分支、worktree 分工不作为新任务默认做法；已有工作树及其改动保留，不自行清理。
- UI 调整不得顺带改变模块归属、风险类型范围、统计口径或业务流程。完成已授权范围；发现需要扩大范围时，先说明具体变化，不把设计参考或旧方案当作额外授权。
- 拉取、合并代码时保留并重新应用本地改动及删除状态；只备份到 stash、未恢复到工作区，不算保留改动后的更新完成。按文件核对合并结果，不能用旧 Demo 覆盖当前业务实现。
- 部署、换库或追加演示数据时保留历史记录、关联关系和审计；换库不能只迁表结构或只补当天数据。追加数据须标明来源，按业务时区及场景补齐目标、计划、研判、事件、通知和回执的必要关联，不修改旧时间冒充实时数据，不补造真实送达。
- 交付用业务语言先说明“在哪个页面、看到了什么变化、现在怎么操作”，再列文件和验证结果。区分代码已改、已部署、模拟验收和真实通道/设备验收。

## 追加数据的跨模块配套（用户确认）

- 用户说“造数据、生成测试数据、补演示数据、刷新今天的数据、MQTT 模拟、补齐业务链”时，自动使用 `generating-business-scenario-data` 技能，无需用户点名；同时读取本文件、`dongying-vue/AGENTS.md` 和下述配套规范。涉及后端时读取同级 `../houtaiguanli` 的适用 AGENTS 与实际接口/状态机。技能不可用时明确说明，并按这些项目规范继续已授权工作。
- 实时目标、轨迹和设备状态场景优先复用现有 MQTT 模拟入口，由后端实际逻辑产生研判、告警及通知任务；计划、航线、接收对象等前置资料通过现有业务接口或受控 seed 配套。历史补录单独处理，气象等非设备来源走自身接入契约；不把所有数据硬塞 MQTT，不直接插入成功结果冒充实时链路通过。明确批次、运行时长/停止条件及模拟通道，不因自动调用技能而扩大真实通道授权。
- 新增、补录或刷新演示数据必须按完整业务场景组织，先列出涉及模块、同一实体的关联 ID、发生顺序、来源和预期状态，再落库；不能只让每个页面各自“有今天的数据”。具体清单见[演示数据跨模块配套规范](docs/演示数据跨模块配套规范.md)。
- 正常自动通知场景须配套触发依据、通知任务、发送记录、接收对象、送达/失败回执、时间和审计。同一事件已有送达结果，后来观测或事件时效失效不能改成“从未发送/自动通知暂停”。未达到发送条件、发送失败、结果未知须保留各自事实，不批量置为成功。
- 历史模拟补录与实时自动执行分开验收。历史补录须明确“模拟历史补录”、场景发生时间和实际补录时间；不能伪称当时真实运行过规则、发送过真实短信或收到真实回执。不得修改旧发生时间、放宽时效、删除未知原因来冒充符合实时发送条件。
- 通知、现场观察、反制授权/执行、交接与处罚分别配套；送达不代表已读、飞离、风险解除、反制获准或案件办结。已冻结交接材料保持原样，迟补记录通过补充材料关联，不回写过去的快照。
- 追加前限定批次并备份相关记录；稳定 ID/幂等键防重，保留人工办理和旧历史。交付前逐链核验数据库关系、时间顺序、列表/详情/跨页一致性、刷新与重新登录，并说明统计使用同源事实还是独立的模拟样本口径。

## 文档维护

- 稳定约束写入两级 `AGENTS.md`；页面现状和入口写 README；接口、状态及验收证据放对应专题文档。阶段报告保留日期，不把旧报告中的“当前”当作今天的实现。
- 后续明确的用户决定替代同一事项的旧设计；未实施建议、一次性“不测试”、某次重启或某晚调度安排不升级为永久规则。历史材料与当前规则冲突时标明适用日期和替代入口，不恢复已删除页面、文件或流程。
- 本轮对话依据、已覆盖规则、补充项和待验证差异见[项目对话与文档核对](docs/项目对话与文档核对-2026-09-16.md)。

## 验收

```powershell
cd dongying-vue
npm run build
node tools/scan.cjs
npx playwright test e2e/admin-migration.spec.js
```

保留用户未提交改动及删除状态，不恢复旧方案、不顺手清理无关文件。交付前执行 `git diff --check`，并如实说明未执行项。
