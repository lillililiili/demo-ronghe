# 需求确认表 · 增补三（2026-09-08）

接续《需求确认表-2026-09-08（增补二）》F1–F5。本次两条都来自飞行计划页"本航线风险"区块的评审，属于业务口径，不是代码问题；在客户答复前，平台按"当前实现"列的演示值运行，并在界面上标"演示值"。

| 编号 | 事项 | 当前实现（演示值，未经确认） | 需要客户确认 | 不确认的影响 |
| --- | --- | --- | --- | --- |
| F6 | **航线风险的时间范围**：一条待执行的计划，提前多久开始把走廊沿线的空中异物（鸟群、气球、风筝、孔明灯等）算作"本航线风险"？结束后多久停止？ | C04 只在"目标在走廊内或 300 m 内"且"计划处于活动时间窗"时生成风险；活动时间窗 = 计划起止时间前后各放宽 15 分钟（参数 `plan_window_pad_min`，DEMO）。也就是说，起飞前 15 分钟之内的待执行计划和正在执行的计划会被评估，更早的待执行计划不评估、不显示风险。 | 1. 起飞前多少分钟开始评估（15 / 30 / 60 分钟，或按报备时间？）；2. 落地后是否还需要保留一段时间；3. 距离阈值 300 m 是否合适；4. 是否需要在计划审批阶段就做一次"沿线异物预检"（这需要历史异物数据源，目前没有）。 | 提前量太短：值班员看到风险时飞机已经起飞；太长：待执行计划页面会堆满与当前无关的风险。 |
| F7 | **执行中计划旁出现风险时的处置**：任务已经在空中，走廊旁出现鸟群或异物，平台只做通报，还是要提示驱离？驱离对象是异物还是通知飞手改航/返航？ | 风险只走"核验 → 通知上级/风险通报"，不触发任何处置动作。驱离（驱鸟炮等）在平台里属于处置授权的一种动作，需要有权限的人发起、另一人审批，目前只对"感知目标"发起，没有和航线风险联动。 | 1. 执行中计划旁的风险是否需要在风险详情里给出"申请驱离"入口；2. 驱离的审批条件与时限（同 F4 的授权条件）；3. 是否同时通知飞手（需要飞手联系渠道，目前没有）；4. 驱离设备的位置与覆盖范围资料（决定能不能驱到）。 | 不确认则维持"只通报不处置"；确认后是接线工作（风险详情加入口、复用处置授权域），不需要改数据模型。 |

补充说明：

- F6 的两个参数（`plan_window_pad_min`、`corridor_near_m`）已在规则参数表里标为 DEMO，客户答复后改参数即可，不改代码。
- F7 若确认要做，工作量在阶段 13 处置授权域之上约半天到一天，取决于是否要通知飞手。
