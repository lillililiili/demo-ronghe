# 内部依据：联调日志模板

> **内部核对用，交付时不随正文发出**（决策 17-1）。
> 锚点用**文件 + 常量名 / 方法名 / 协议小节**，不用行号。
> 检索时间：2026-09-09。

## 每一条说法的出处

| 模板里的说法 | 出处 |
| --- | --- |
| 各路的主题格式与字段名 | `设备资料/凌云协议/` 下协议 A 一、三；协议 B 一、二；协议 C 1.1、2 |
| AOA 的目标经纬度和高度无效、用方位角 | 协议 A 三、AOA 设备 |
| 光电类别「优先支持 drone、bird」 | 协议 C 2.3 |
| 心跳至少每秒一次、30 秒收不到工参视为离线 | 协议 C 2.2；协议 A 四 |
| `CameraStatus` 支持列与备注矛盾 | 协议 C 2 协议事件表 |
| 响应序号与请求序号相同 | 协议 B 1.1 消息头 |
| 「该指令码对应的设备类型缩写尚未确认，不能下发」 | `modules/device/application/LingyunControlService` |
| 报文原样存 `inbox_message`、按哈希反查、`last_error` 与重试次数 | `docs/直连接入计划/凌云回放说明.md`；`modules/fusion/infrastructure/FusionInboxRepository` |
| 回放文件的发布方法与「不要再序列化一次」 | `docs/直连接入计划/凌云回放说明.md` |
| 未受理的设备类型、未实现的协议 C 事件 | 《协议缺口矩阵》第一、三节 |

## 一处判断，请复核

正文第九节保留了针对 `inbox_message` 的查询语句。判断依据：那是**联调当天的操作步骤**（现场工程师照着敲），
不是"这句话的出处"，去掉这一节就没法用了。若认为表名也算内部引用，可以改成由我们的人执行、
模板里只留"向平台侧索取该报文的接收记录"——请示下。
