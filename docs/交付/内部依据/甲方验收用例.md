# 内部依据：甲方验收用例

> 屏幕文字于 2026-09-10 按当时的页面重新逐字核对过一遍（阶段 18 的"整项目枝行走"改了不少占位文案）。

不随交付件外发。锚点用「文件 + 方法名/常量名/契约小节」，不用行号——行号会随别的改动漂移。

| 正文里的说法 | 出处 |
| --- | --- |
| 十四个业务页面 + 三个独立入口（登录、修改密码、大屏） | 会议纪要第十一节；`dongying-vue/src/config/navModel.js` 的 `NAV` 与 `ROUTES` |
| 工作台顶部五个方块的名称与计数 | `dongying-vue/src/pages/WorkbenchPage.vue` 模板中的 `.wb-kpis` 一段；计数来自 `workbenchEvents.js` 的 `workbenchStats` |
| 三类事项的中文名（无人机告警 / 飞行计划风险 / 设备告警） | `dongying-vue/src/services/workbenchEvents.js` 的 `kindLabel` |
| 每条事项的"下一步"、灰按钮原因与提示语 | `dongying-vue/src/services/workbenchEvents.js` 的 `nextStep` |
| 无人机事件流程条四步（告警接收 / 人工核实 / 反制 / 通知处罚部门） | `workbenchEvents.js` 的 `uavSteps` |
| 反制环节"未接入"与"尚无授权"分开说 | `workbenchEvents.js` 的 `uavSteps` 内 `counterStep`；`latestCountermeasure` 读失败返回 undefined，与"没有授权"返回 null 区分 |
| 风险流程条三步与"已排除" | `workbenchEvents.js` 的 `riskSteps` |
| 设备异常流程条四步与各步名称 | `workbenchEvents.js` 的 `deviceSteps` |
| 三个核实结论的名称与后果 | `dongying-vue/src/ui/uavVerificationModal.js` 的 `conclusion` 字段 options |
| 批准人不能是申请人 | `server/.../modules/disposal/domain/DisposalRules.java` 的 `requireTwoPerson` |
| 复核人不能是承办人 | `server/.../modules/punishment/domain/PunishmentRules.java` 的 `requireDifferentReviewer` |
| 处置动作按钮由服务端逐条下发（状态 + 权限都满足才给） | `DisposalRules.java` 的 `allowedActions`；页面侧 `dongying-vue/src/pages/PunishPage.vue` 的 `DISPOSAL_ROW_ACTIONS` / `disposalRowActions` |
| 审批弹窗的结论（批准/驳回）、驳回必填意见、有效期提示 | `dongying-vue/src/ui/disposalAuthModal.js` 的 `openDisposalApproval` |
| 执行等设备回执、人工通道改为"登记执行结果"、执行受阻如实说明 | `disposalAuthModal.js` 的 `openDisposalExecution` / `openDisposalManualResult` |
| 停止会撤销授权并尝试急停，设备不支持急停时授权仍停止 | `disposalAuthModal.js` 的 `openDisposalStop`；`DisposalRules.java` 的 `deviceStopResult` |
| 重启原因必填、"模拟回执不代表真实设备已重启"、离线或协议不支持时失败 | `dongying-vue/src/pages/WorkbenchPage.vue` 的 `openDeviceReboot` |
| 恢复校验通过/未通过的提示 | `WorkbenchPage.vue` 的 `runDeviceAction` |
| 无工作台查看权限时的整块提示，且不反复发请求 | `WorkbenchPage.vue` 的 `forbidden` 标记与 `loadQueue` 的 403 分支 |
| "已接入 / 无读取权限 / 设备归属映射未配置" | `workbenchEvents.js` 的 `AVAILABILITY_LABEL` |
| 风险通知不选接收方、按默认接收方发出 | `dongying-vue/src/pages/FlightsPage.vue` 的 `openRiskNotify`（`fields: []`，请求体不带 `recipient_id`）；服务端 `HandoffSubmissionService` 解析默认接收方 |
| 回执"已驱离"后风险转"已通知" | 2026-09-10 在验收库实测：通知 `275e7c41…` 为 `DELIVERED/ACKNOWLEDGED/DISPERSED`，`风险-0907-003` 随之 `state=NOTIFIED`（`.superpowers/sdd/task-18.6-joint.md` 复验一节） |
| 通报记录的"已回执 · 已驱离" | `FlightsPage.vue` 的 `receiptText`；`ui/labels.js` 的 `RECEIPT_RESULT_LABEL` |
| 同一风险重复提交会被拒并切到通报记录 | `FlightsPage.vue` 的 `openRiskNotify` 中 `HANDOFF_ALREADY_EXISTS` 分支；2026-09-10 实测 409 |
| 演示环境的回执由平台自带的模拟渠道给出、真实上级系统未对接 | `server/.../modules/handoff/infrastructure/MockSuperiorHandoffChannel.java`（`app.handoff.channel=mock` 时启用，风险通知一律回"已驱离"）与 `NoHandoffChannel.java`（缺省，投递停在待投递并标未接通） |
| 处罚交接仍需选接收方 | `AlarmsPage.vue` 的 `punishModal`（接收方下拉未改） |
| "处置授权功能暂不可用" | `dongying-vue/src/ui/disposalAuthModal.js` 的 `DISPOSAL_UNAVAILABLE_TEXT` |
| "处罚案件功能暂不可用" | `dongying-vue/src/services/punishmentApi.js` 的 `PUNISHMENT_UNAVAILABLE_TEXT` |
| "案件只在本平台内办理，不会自动报送外部处罚系统"、决定书与复核结论两条同类说明 | `dongying-vue/src/pages/PunishPage.vue` 的 `BLOCK_GAPS`（`case` / `doc` / `review`） |
| 处罚办理的按钮与顺序（立案→指派→拟定裁量→确认裁量→提交复核结论→生成决定书→结案，另有撤案、作废、新增待补线索、标记已补齐、预览全文、复制全文） | `PunishPage.vue` 模板中的按钮文案；弹窗在 `dongying-vue/src/ui/punishmentModals.js` |
| "当前账号没有证据查看权限，无法汇总证据链。"且不发该请求 | `dongying-vue/src/pages/AlarmsPage.vue` 的 `loadChain` |
| 告警页六个指标的名称 | `AlarmsPage.vue` 的 `KPI_DEFS` |
| 告警页可按四个键排序、可导出 | `AlarmsPage.vue` 的 `SORT_KEYS` 与 `exportCsv`；`dongying-vue/src/services/alarmApi.js` 的 `exportAlarmsCsv` |
| 风险列表可筛选与导出 | `dongying-vue/src/pages/FlightsPage.vue` 的风险页签工具条与 `exportRiskCsv`；`dongying-vue/src/services/riskApi.js` 的 `exportRisksCsv` |
| 区域字典读不到时下拉只剩"不可用"并在提示里说明 | `AlarmsPage.vue` 的 `regionOpts` / `paintRegionOptions`；`FlightsPage.vue` 的 `riskDistrictOptions` / `riskDistrictTitle` |
| 飞行计划页六个指标含"鸟类事件""涉及航线" | `FlightsPage.vue` 的 `RISK_KPI_QUERIES` 与 `loadRoutesInvolved` |
| 飞行计划详情不再展示审批信息（该区块已撤） | `dongying-vue/src/pages/FlightsPage.vue`（全文已无"审批信息"一节） |
| 计划列表没有导出入口，可导出的只有告警列表与风险列表 | `FlightsPage.vue` 只有风险页签的 `exportRiskCsv`；`alarmApi.exportAlarmsCsv` / `riskApi.exportRisksCsv` |
| 合法性页缺项写"起降点未知""飞手/单位未知"、地图写"无可信几何，地图不绘制：…"；"转入处置"入口已撤（只剩一句代码注释） | `dongying-vue/src/ui/legalityReviewModal.js` 的 unknown 原因字典；`LegalityPage.vue` 的 `evidenceMapNote` 与动作区 |
| "驱鸟处置"按钮禁用、提示"设备指令与作业参数待设备方确认"，点击提示"驱鸟处置暂不可用" | `dongying-vue/src/pages/spacerisk/SpaceRiskPage.vue` 详情动作区与 `scareNotAvailable` |
| "实时视频"按钮禁用、提示"协议未提供实时视频流"；"通知机场/周边"入口已不存在 | `dongying-vue/src/pages/SituationPage.vue` 的 `tipActions`（`videoBtn` 一段；全仓已无"通知机场"字样） |
| 悬浮卡三行缺项不渲染 | `SituationPage.vue` 的 `tipSummaryRows` |
| 只报方位的目标画方位线、设备无坐标则不画 | `dongying-vue/src/services/situationData.js` 的 `bearingOrigins` / `attachBearing`；`.superpowers/sdd/task-15.2-report.md` |
| 空域的孔不填充（孔是可飞区） | `dongying-vue/public/assets/js/map.js` 的 `_paintLayers` 空域一段（even-odd 填充）；`situationData.js` 的 `polygonRings` / `toAirspaces`；`.superpowers/sdd/task-16.2-report.md` |
| 告警页页尾"实时视频协议未提供；轨迹回放仅定位已加载轨迹点，不是视频。" | `AlarmsPage.vue` 页尾说明区 |
| 无权访问页的文案 | `dongying-vue/src/pages/AccessDeniedPage.vue` |
| 可达页面以服务端下发的菜单为准 | `dongying-vue/e2e/support/matrix.js` 的 `expectReachable` |
| 角色页动作权限分区、未改动的行不提交 | `dongying-vue/src/pages/RolesPage.vue` 的 `actionDraft` / `actionsDirty`；`.superpowers/sdd/task-15.2-report.md` |
| 证据页的列与动作（取证时刻、大小、关联对象、下载、冻结、解除冻结、哈希一致/校验失败、文件已销毁台账保留） | `dongying-vue/src/pages/EvidencePage.vue` |
| 设备调测的"不可判定""不可测" | `dongying-vue/src/pages/CommissionPage.vue` |
| 设备实时监测的两条读取失败提示 | `dongying-vue/src/pages/MonitorPage.vue` |
| 统计接口返回"本页为模拟"的标记、但页面未显示 | `dongying-vue/src/services/statsApi.js` 的 `mapOperations`（`simulated` / `sourceMode`）；`dongying-vue/src/pages/StatsPage.vue` 中无对应渲染 |
| 需要改密的账号登录后先跳修改密码页 | `dongying-vue/src/services/auth.js` 的 `needsPasswordChange`；`dongying-vue/src/router/index.js` 的 `beforeEach` |
| 两个账号的菜单与动作权限 | 2026-09-09 实测 `/auth/me`：admin1 十五项菜单、四十二项动作；reviewer1 六项菜单，动作含处置批准/执行/停止与处罚复核 |
| 演示库数据量（告警 25、风险 20、计划 24、目标 50、设备 16、交接 20、案件 7、授权 21、证据 12、工作台 54 = 24+20+10） | 2026-09-09 实测本地演示库（`local` 配置 + 演示播种） |
| 处置审批链在页面上实际跑通过 | `.superpowers/sdd/task-15.2-report.md` 末节（reviewer1 在页面上批准一条授权，状态与版本均已回读核对） |

## 核对范围的限制

- 阶段计划指定的验收库在核对当天没有实例在运行；核对是在另一套 `local` 配置的演示库上用只读请求完成的，**没有对该库做任何写操作**。
- 因此文中的"数量与编号"只代表核对当天那套库；用例流程本身与库无关。
- A 组三条闭环里的写操作步骤（核实、申请、审批、执行、立案、复核、结案、重启、恢复校验）本次**没有**在别人的库上重复执行，依据的是各页面代码与阶段 15、16 的实测记录。
